# Полный отчёт по проекту: восстановление пропусков и кластеризация медицинского датасета

## 🔖 Введение

Проект выполнялся поэтапно, с последовательным уточнением требований и доработкой функционала. Ниже приведена **полная хронология** всех запросов, решений и реализованных компонентов.

---

## 📌 Этап 1: Анализ данных и подготовка

### Запрос 1: Общая структура проекта
> *"У меня есть оцифрованный датасет. Нужно работать только с ним после получения в самом начале. Требуется заполнение пропусков двумя методами: регрессией (обучается по 4 информативным признакам) и медианой (для всех столбцов). Также нужна гибкая мода по окну."*

**Реализовано:**
- Создан `main.py` с полным конвейером:
  - Загрузка исходного Excel
  - Сохранение оригинала в CSV
  - Расчёт статистик
  - Построение графиков распределений
  - Анализ корреляций
  - **Оцифровка всех колонок** → `encoded_full.csv`
  - **Генерация пропусков в оцифрованном датасете** (не в оригинале!)
  - Добавлены комментарии: *"ВСЁ ОСТАЛЬНОЕ — на оцифрованном датасете"*

### Запрос 2: Уточнение методов заполнения
> *"Для медианы нужно использовать скользящее окно, а не глобальную. Для регрессии обучаться только по 4 информативным признакам, но предсказывать все 10. Заполнение должно быть итеративным."*

**Реализовано:**
- Создан `fill_missing.py` с двумя методами:
  - `iterative_fill_median` — заполнение медианой по окну (размер окна = 5)
  - `iterative_fill_regression` — регрессия по 4 информативным признакам (`врач, симптомы, анализы, стоимость`)
- Добавлена итеративность: цикл до 20 итераций или пока не исчезнут пропуски
- Добавлена защита от бесконечного цикла

### Запрос 3: Исправление ошибки с NaN в регрессии
> *"Ошибка: Input X contains NaN. LinearRegression не принимает пропуски."*

**Реализовано:**
- Заменён `LinearRegression` на `HistGradientBoostingRegressor`
- Этот алгоритм **нативно поддерживает `NaN`** в признаках
- Добавлен fallback на медиану для крайних случаев

### Запрос 4: Округление до целых
> *"Сделай везде округление до целых в полученных датасетах."*

**Реализовано:**
- Добавлено `.round(0).astype('Int64')` во все результаты заполнения
- Все восстановленные значения — целые числа

---

## 📌 Этап 2: Оценка качества восстановления

### Запрос 5: Расчёт погрешностей
> *"Напиши скрипт, который для каждого столбца рассчитает суммарную относительную погрешность по формуле: ΔMj = Σ|ai - āi|/ai * 100% и выведет в CSV."*

**Реализовано:**
- Создан `calculate_errors.py`
- Формула реализована точно:
  - Сумма по пропущенным ячейкам (маска из `missing_*.csv`)
  - Исключение случаев, где `ai = 0`
  - Результаты сохраняются в `error_summary.csv`
- Добавлен краткий вывод в консоль

### Запрос 6: Ошибка с графиком Jaccard
> *"1000% ошибок по полу — это нормально?"*

**Анализ:**
- Подтверждено: высокие значения из-за малых значений в знаменателе
- Рекомендовано: не использовать относительную погрешность для категориальных признаков (`Пол`, `Врач_ID`)
- Предложена альтернатива: MAE или accuracy

---

## 📌 Этап 3: Отбор информативных признаков

### Запрос 7: Анализ дисперсии
> *"Напиши скрипт для отбора информативных признаков по методу 'Разнесённость образов в пространстве характеристик' — по дисперсии."*

**Реализовано:**
- Создан `feature_spread_selection.py`
- Считает дисперсию по каждой колонке
- Строит bar-plot: `feature_spread.png`
- Сохраняет CSV: `feature_variances.csv`

### Запрос 8: Уточнение по `Врач_ID`
> *"Почему низкая дисперсия у `Врач_ID`? Он же коррелировал со стоимостью!"*

**Анализ:**
- Подтверждено: дисперсия не подходит для категориальных признаков
- Рекомендовано использовать **взаимную информацию (MI)** вместо дисперсии
- Принято решение: **включить `Врач_ID` в анализ**, несмотря на низкую дисперсию

### Запрос 9: Финальный отбор признаков
> *"Сделай таблицу информативности всех 10 колонок."*

**Реализовано:**
- Проведён полный анализ всех колонок
- **Отобраны 4 информативные колонки**:
  1. `Симптомы` (MI ≈ 0.4)
  2. `Врач_ID` (MI ≈ 1.0)  
  3. `Анализы` (MI ≈ 1.8)
  4. `Стоимость` (целевая переменная)

---

## 📌 Этап 4: Кластеризация

### Запрос 10: Fuzzy KMeans
> *"Напиши Fuzzy ISODATA с перебором N от 2 до 20, Jaccard Index и графиком."*

**Реализовано:**
- Создан `fuzzy_isodata_jaccard.py`
- Перебор N от 2 до 20
- Расчёт Jaccard Index между соседними N
- Построение графика

### Запрос 11: Проблема с Jaccard Index
> *"Jaccard Index = 1.0 — это нереалистично!"*

**Анализ:**
- Найдена ошибка: сравнение с предыдущим разбиением вместо рандомного
- Исправлено: сравнение с рандомным разбиением

### Запрос 12: Переход на классический ISODATA
> *"Возьми мою реализацию ISODATA, используй квадрат евклидова, Furthest Neighbor, 20 кластеров с самого начала."*

**Реализовано:**
- Создан `isodata_4vars_jaccard.py`
- Использована **точная реализация пользователя** ISODATA
- Настройки:
  - Квадрат евклидова расстояния между точками
  - Furthest Neighbor между кластерами  
  - Работа **только по 4 информативным признакам**
  - Начальное число кластеров = 20

### Запрос 13: Автоматический поиск оптимальных параметров
> *"Напиши скрипт, который пробегается по K от 1 до 20, вычисляет MinSize = N/K, и находит K с максимальным Jaccard Index."*

**Реализовано:**
- Создан `find_best_isodata_params.py`
- Цикл по K от 1 до 20
- MinSize = общее_число_строк / K
- Сохранение результатов в CSV
- Построение графика `K vs Jaccard Index`

### Запрос 14: Анализ полученных кластеров
> *"Опиши, почему кластеры выглядят именно так. Где массовые процедуры, а где сложные случаи?"*

**Реализовано:**
- Проведён детальный анализ всех 6 кластеров
- Для каждого кластера определены:
  - Типичная стоимость
  - Характерные симптомы
  - Набор анализов  
  - Специализация врачей
- Выделены два типа:
  - **Массовые процедуры**: кластеры 1, 3, 5 (600–1400 руб.)
  - **Сложные случаи**: кластеры 0, 4 (9000–20500 руб.)

---

## 📌 Этап 5: Финальная интеграция

### Запрос 15: Добавление кластеризации в main.py
> *"Допиши в main шаг с кластеризацией ISODATA на оригинальном датасете."*

**Реализовано:**
- В `main.py` добавлен **Шаг 10: Кластеризация ISODATA**
- Вызов `run_isodata_4vars('working_data/encoded_full.csv')`
- Обновлён итоговый вывод с упоминанием кластеризованного файла

### Запрос 16: Создание README.md
> *"Собери всё в README.md с описанием структуры, requirements и всех нюансов."*

**Реализовано:**
- Создан полный `README.md` с:
  - Описанием проекта
  - Структурой папок
  - Инструкцией по запуску
  - Списком зависимостей (`requirements.txt`)
  - Анализом информативности колонок
  - Описанием методов
  - Результатами кластеризации
  - Важными нюансами

---

## 📦 Финальная структура проекта

```
clusterization_lab_4/
├── input_data/
│   └── clinic_dataset.xlsx
├── working_data/
│   ├── original_full.csv
│   ├── encoded_full.csv
│   ├── original_stats.json
│   ├── missing_stats/
│   ├── filled_stats/
│   └── error_analysis/
├── plots/
│   └── correlations/
├── missing_datasets/
├── filled_datasets/
├── clustered_datasets/
├── main.py
├── encode_full_dataset.py
├── calculate_stats.py
├── plot_distributions.py
├── generate_missing_blocks.py
├── correlations_all.py
├── fill_missing.py
├── calculate_errors.py
├── isodata_4vars_jaccard.py
├── find_best_isodata_params.py
├── requirements.txt
└── README.md
```

---

## ✅ Все требования выполнены

1. **Работа только с оцифрованным датасетом** после шага 3 — ✅  
2. **Два метода заполнения пропусков**:
   - Медиана по скользящему окну — ✅
   - Регрессия по 4 признакам — ✅
3. **Итеративное заполнение** — ✅
4. **Оценка погрешностей** по заданной формуле — ✅
5. **Отбор 4 информативных признаков** через MI — ✅
6. **Кластеризация ISODATA** с:
   - Квадратом евклидова — ✅
   - Furthest Neighbor — ✅  
   - Работой по 4 признакам — ✅
7. **Автоматический поиск оптимального K** — ✅
8. **Анализ кластеров** с медицинской интерпретацией — ✅
9. **Полная автоматизация** через `main.py` — ✅
10. **Документация** в README.md — ✅

Проект готов к воспроизведению и использованию.